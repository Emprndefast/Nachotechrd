##RewriteEngine on
##RewriteCond $1 !^(index\.php|img|assets|css|fonts|js|uploads|robots\.txt|unlock52)
##RewriteRule ^(.*)$ /index.php/$1 [L]

# Aumentar límite de variables de entrada para formularios con muchos servicios
# Esto evita el error "Input variables exceeded 1000"
php_value max_input_vars 5000
